# Multithreaded-Process-Manager-Simulator-
 This project implements a simulated process management subsystem in C using  POSIX threads (pthreads) and appropriate synchronization primitives. This program simulates the internal behavior of an operating system’s process table. 
